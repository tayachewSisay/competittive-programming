class Solution(object):
    def sortSentence(self, s):

        result = ""
        myarray = s.split()

        for i in range(1, 10):

            for word in myarray:

                if word[-1] == str(i):
                    result += " " + word[:-1]

        return result.strip()
